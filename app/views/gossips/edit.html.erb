<h1>Editer le gossip</h1>

<%= form_tag gossip_path(@gossip), method: :put do %>
  <%= render 'form' %>
  <%= submit_tag "Modifier le gossip"%>
<% end %>

<h3>Effacer à jamais le gossip</h3>
<%= link_to 'Cliquer pour détruire', @gossip, method: :delete %>
<br>
<%= link_to 'Retour', gossip_path %>
